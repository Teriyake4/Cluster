apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: longhorn-volv-test-pvc
  namespace: longhorn-test
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
