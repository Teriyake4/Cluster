apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: open-terminal-pvc
  namespace: open-webui
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
