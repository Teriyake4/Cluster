apiVersion: v1
kind: Service
metadata:
  name: steel-service
  namespace: open-webui
spec:
  selector:
    app: steel
  ports:
    - name: api
      port: 3000
      targetPort: 3000
    - name: cdp
      port: 9223
      targetPort: 9223
  type: ClusterIP
